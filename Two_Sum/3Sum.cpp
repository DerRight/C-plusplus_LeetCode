#include <iostream>
#include <vector>
#include <unordered_map>
using namespace std;

vector<vector<int>> Solution_3Sum(vector<int>& nums) {
	unordered_map<int, int> numMap;

	for (int i = 0; i < nums.size(); i++) {
		int complement;
	}
}

int main() {
	vector<int> nums = { -1, 0, 1, 2, -1,-4 };
	Solution_3Sum(nums);

	return 0;
}